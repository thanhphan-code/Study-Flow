# Feature modules

Each feature owns its API calls, components, hooks, schemas, store, and types. Cross-feature UI belongs in `components`; generic infrastructure belongs in `api`, `services`, or `utils`.

Planned MVP modules: auth, subjects, studySets, flashcards, quizzes, study, progress, exams, and ai.
