export type ReviewRating = 'Again' | 'Hard' | 'Good' | 'Easy'
export type FlashcardStatus = 'New' | 'Learning' | 'Reviewing' | 'Mastered'
export type FlashcardProgress = { id: string; flashcardId: string; status: FlashcardStatus; correctCount: number; wrongCount: number; lastReviewedAt: string; nextReviewAt: string; intervalDays: number; easeFactor: number; masteryScore: number; weaknessScore: number; recommendedNextAction: 'LearnNew' | 'ReviewNow' | 'Strengthen' | 'KeepFresh'; recommendedLearningStep: { activity: 'Recall' | 'MultipleChoice' | 'TrueFalse' | 'Application' | 'SimilarQuestion'; direction: 'Forward' | 'Reverse'; dueAt: string | null; reason: string; priority: number }; stateRevision: number; schedulerVersion: string; lastScheduledAt: string | null }
export type DueFlashcard = { flashcardId: string; studySetId: string; frontText: string; backText: string; explanation: string | null; languageCode: string | null; readingText: string | null; romanization: string | null; exampleText: string | null; exampleTranslation: string | null; memoryTip: string | null; nextReviewAt: string; status: FlashcardStatus }
